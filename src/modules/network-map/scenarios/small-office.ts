/**
 * Scenario 2 — Small office.
 *
 * The home LAN once somebody has to take it seriously: a managed switch, three VLANs, a
 * firewall acting as the layer-3 gateway, a local resolver, a NAS, and a printer.
 *
 * The data itself lives in `@/core/topologies`; see `./home-lan.ts` for why.
 */

export { SMALL_OFFICE } from '@/core/topologies';
