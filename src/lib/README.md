# `src/lib` — small shared utilities

Tiny, dependency-light helpers used across layers: `cn()`, formatters, id generation.

Nothing with domain meaning belongs here — protocol logic goes in `@/core`, UI goes in
`@/components`.
