import { moduleContract } from '../helpers';

/**
 * `dns-explorer` in a browser: the contract every simulating module shares, then anything that
 * is this module's own. Wave 3's pass for this module edits this file and no other spec.
 */
moduleContract('dns-explorer');
